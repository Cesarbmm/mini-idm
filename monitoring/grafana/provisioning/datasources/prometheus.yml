apiVersion: 1

prune: true

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://127.0.0.1:9090
    isDefault: true
    editable: false

    jsonData:
      prometheusType: Prometheus
      prometheusVersion: 2.45.0
      timeInterval: 15s
